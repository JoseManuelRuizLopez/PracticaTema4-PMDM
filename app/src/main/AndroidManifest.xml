<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practicatema4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SeleccionandoImagenes"
            android:label="@string/nombreSeleccionandoImagenes"/>
        <activity
            android:name=".DesplazandoImagenes"
            android:label="@string/nombreDesplazandoImagenes" />
        <activity
            android:name=".JuegoAciertos"
            android:label="@string/nombreJuegoAciertos" />
        <activity
            android:name=".NumerosPrimos"
            android:label="@string/nombreNumerosPrimos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>